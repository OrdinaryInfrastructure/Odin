mode: MainLine # Only add this if you want every version to be created automatically on your main branch.
major-version-bump-message: "^(major-v)"
minor-version-bump-message: "^(minor-v)"
patch-version-bump-message: "^(fix|patch)"
no-bump-message: "^(none|skip)"
tag-prefix: "v"
